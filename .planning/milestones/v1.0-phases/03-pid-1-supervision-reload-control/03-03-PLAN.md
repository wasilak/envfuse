---
phase: 03-pid-1-supervision-reload-control
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - tests/integration/pid1_supervision_reload_test.go
  - internal/config/config.go
  - internal/supervisor/model.go
  - internal/supervisor/supervisor.go
  - internal/supervisor/supervisor_test.go
autonomous: true
requirements: [RELO-03]
must_haves:
  truths:
    - "Runtime enforces configurable restart cooldown with safe default 10s."
    - "When fingerprint changes during cooldown, restart is coalesced into exactly one pending restart after cooldown ends."
    - "Runtime avoids restart loops under rapid successive changes."
    - "Forced-kill during restart path records reason and supervision loop continues deterministically."
  artifacts:
    - path: internal/config/config.go
      provides: "reload_cooldown config schema with default 10s"
    - path: internal/supervisor/supervisor.go
      provides: "cooldown state machine with pending-change coalescing"
    - path: internal/supervisor/model.go
      provides: "reason emission for forced_kill/config_changed transitions"
    - path: tests/integration/pid1_supervision_reload_test.go
      provides: "e2e proof of cooldown and coalesced single deferred restart"
  key_links:
    - from: internal/config/config.go
      to: internal/supervisor/supervisor.go
      via: "reload_cooldown runtime control"
      pattern: "reload_cooldown|10s"
    - from: internal/supervisor/supervisor.go
      to: restart path
      via: "pending-change bit drained after cooldown"
      pattern: "pendingRestart|cooldown"
    - from: internal/supervisor/supervisor.go
      to: reason taxonomy
      via: "forced kill and config change events"
      pattern: "forced_kill|config_changed"
---

## Phase Goal

**As a** platform operator, **I want to** have reload events coalesced with a cooldown window, **so that** rapid secret changes do not create restart storms.

<objective>
Deliver the final vertical slice of Phase 3: cooldown-based anti-loop restart coalescing.

Purpose: implement D-09..D-12 plus RELO-03 without reducing locked scope.
Output: cooldown config + runtime coalescing behavior + tests proving no loop under rapid change churn.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pid-1-supervision-reload-control/03-CONTEXT.md
@.planning/phases/03-pid-1-supervision-reload-control/03-RESEARCH.md
@.planning/phases/03-pid-1-supervision-reload-control/03-PATTERNS.md
@.planning/phases/03-pid-1-supervision-reload-control/03-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for cooldown window and deferred single restart coalescing (RED)</name>
  <read_first>
    - internal/supervisor/supervisor_test.go
    - tests/integration/pid1_supervision_reload_test.go
    - internal/config/config.go
  </read_first>
  <files>
    - internal/supervisor/supervisor_test.go
    - tests/integration/pid1_supervision_reload_test.go
  </files>
  <behavior>
    - Test 1: restart within cooldown is suppressed and marked pending.
    - Test 2: multiple changes during cooldown coalesce to one restart after cooldown.
    - Test 3: default cooldown value is 10s when unset.
  </behavior>
  <action>
    Create failing unit/integration tests that encode D-09..D-12 explicitly: cooldown is primary anti-loop control, pending flag is set on change inside cooldown, and exactly one deferred restart executes after cooldown. Include assertion for default `reload_cooldown=10s` and configurable override path. Analog anchors from `03-PATTERNS.md`: unit tests in `internal/supervisor/supervisor_test.go` follow `internal/sync/coordinator_test.go` scripted deterministic assertions + explicit status/reason checks; integration tests in `tests/integration/pid1_supervision_reload_test.go` follow `tests/integration/dual_vector_delivery_test.go` CLI harness and bounded-output assertion structure.
  </action>
  <verify>
    <automated>go test ./internal/supervisor ./tests/integration -run 'Cooldown|Coalesc|DeferredRestart' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Tests explicitly check restart counts (e.g., expected 1 deferred restart for N changes during cooldown).
    - Tests fail before cooldown logic implementation.
    - Tests include both default and custom cooldown configuration paths.
  </acceptance_criteria>
  <done>RED tests capture anti-loop semantics as measurable behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reload cooldown state machine with pending-change coalescing</name>
  <read_first>
    - internal/config/config.go
    - internal/supervisor/supervisor.go
    - internal/supervisor/model.go
  </read_first>
  <files>
    - internal/config/config.go
    - internal/supervisor/supervisor.go
    - internal/supervisor/model.go
  </files>
  <behavior>
    - Test 1: cooldown prevents immediate subsequent restarts.
    - Test 2: one pending restart is executed once cooldown expires.
    - Test 3: forced-kill during restart still records reason and supervision continues.
  </behavior>
  <action>
    Extend config with `reload_cooldown` using default `10s` per D-12 and validation. Implement cooldown gating in supervisor: after a restart, hold cooldown window (D-09); if changed fingerprint arrives during cooldown, set pending flag (D-10); on cooldown expiry, execute exactly one restart if pending and clear flag. Keep implementation tied to configured cooldown knob (D-11). Preserve D-04 behavior: if restart path requires forced kill, record `forced_kill` reason and continue deterministic loop. Analog anchors from `03-PATTERNS.md`: strict config validation + unknown-field rejection from `internal/config/config.go` shared pattern; deterministic decision inputs from shared `Deterministic ordering before externally visible decisions`; success-gated transitions from shared `Success-only commit gate`; explicit outcome taxonomy style from `internal/sync/model.go` mapped to supervisor reasons.
  </action>
  <verify>
    <automated>go test ./internal/supervisor -run 'Cooldown|Pending|ForcedKill' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `reload_cooldown` exists in config and defaults to `10s`.
    - Supervisor performs at most one deferred restart for multiple in-cooldown changes.
    - Pending flag is cleared after deferred restart execution.
    - Forced-kill reason is emitted when timeout escalation occurs in restart flow.
  </acceptance_criteria>
  <done>RELO-03 anti-loop behavior is implemented with locked decision fidelity.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize integration assertions for no-loop guarantees under rapid churn</name>
  <read_first>
    - tests/integration/pid1_supervision_reload_test.go
    - internal/supervisor/supervisor.go
    - internal/fingerprint/fingerprint.go
  </read_first>
  <files>
    - tests/integration/pid1_supervision_reload_test.go
  </files>
  <action>
    Add high-signal integration scenario that simulates rapid successive effective changes and verifies restart count follows cooldown/coalescing rules (no restart storm). Assert reason taxonomy includes `config_changed` for restart trigger and `forced_kill` when timeout path is exercised. Analog anchors from `03-PATTERNS.md`: integration harness/fixture/assertion style from `tests/integration/dual_vector_delivery_test.go`, plus shared deterministic outcome/taxonomy assertions used in `internal/sync/coordinator.go` + `internal/sync/model.go` so restart counts and reasons are explicit and reproducible.
  </action>
  <verify>
    <automated>go test ./tests/integration -run 'TestPID1_(CooldownCoalescesRestarts|NoRestartLoopUnderRapidChanges)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Integration tests verify bounded restart count under rapid updates.
    - Assertions include reason taxonomy checks, not just timing behavior.
    - Tests are deterministic (no flaky sleep-only assertions without bounded conditions).
  </acceptance_criteria>
  <done>Phase 3 anti-loop guarantees are validated end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rapid change events -> restart controller | Frequent change signals can drive self-DoS if unbounded |
| cooldown config -> lifecycle behavior | Misconfiguration can disable anti-loop protections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Denial of Service | cooldown/restart controller | mitigate | Enforce cooldown + pending coalescing (D-09, D-10) and test bounded restart counts. |
| T-03-10 | Tampering | cooldown configuration | mitigate | Validate `reload_cooldown` duration; default to safe `10s` when absent (D-11, D-12). |
| T-03-11 | Repudiation | restart/kill event trail | mitigate | Emit and test `config_changed` and `forced_kill` reasons for lifecycle transitions. |
</threat_model>

<verification>
- `go test ./internal/supervisor ./tests/integration -run 'Cooldown|Coalesc|RestartLoop' -count=1`
- `go test ./...`
</verification>

<success_criteria>
- RELO-03 complete with cooldown and coalescing semantics.
- Locked decisions D-09, D-10, D-11, D-12 implemented exactly.
- Phase 3 restart-loop prevention is test-proven under rapid change scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pid-1-supervision-reload-control/03-03-SUMMARY.md`
</output>

---
phase: 03-pid-1-supervision-reload-control
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/integration/pid1_supervision_reload_test.go
  - internal/fingerprint/fingerprint.go
  - internal/fingerprint/fingerprint_test.go
  - internal/sync/model.go
  - internal/sync/coordinator.go
  - internal/supervisor/model.go
  - internal/supervisor/supervisor.go
autonomous: true
requirements: [RELO-01, RELO-02]
must_haves:
  truths:
    - "When effective config changes are applied successfully, the supervised workload restarts once with reason `config_changed`."
    - "When a cycle completes successfully but effective config is unchanged, the workload keeps running without a restart."
    - "Metadata-only differences (for example timing/fetch-order variance) do not trigger a workload restart."
    - "Equivalent config content produces consistent restart/no-restart behavior across repeated cycles."
  artifacts:
    - path: internal/fingerprint/fingerprint.go
      provides: "canonical sort + SHA-256 digest over env + rendered files"
    - path: internal/supervisor/supervisor.go
      provides: "restart gate that compares candidate fingerprint with last applied fingerprint"
    - path: internal/sync/coordinator.go
      provides: "candidate state handoff used for pre-commit fingerprinting"
    - path: tests/integration/pid1_supervision_reload_test.go
      provides: "end-to-end restart-on-change and no-restart-on-no-change proofs"
  key_links:
    - from: internal/sync/coordinator.go
      to: internal/fingerprint/fingerprint.go
      via: "candidate env/files passed to hash before commit"
      pattern: "fingerprint\\.Compute|candidate"
    - from: internal/fingerprint/fingerprint.go
      to: internal/supervisor/supervisor.go
      via: "restart decision input"
      pattern: "lastFingerprint|candidateFingerprint"
    - from: internal/supervisor/supervisor.go
      to: child restart path
      via: "successful changed-state cycles only"
      pattern: "config_changed"
---

## Phase Goal

**As a** platform operator, **I want to** have envfuse restart my workload only when effective config actually changes, **so that** reload behavior is deterministic and avoids unnecessary disruption.

<objective>
Deliver the second vertical slice of Phase 3: deterministic fingerprinting and restart gating on successful changed-state cycles.

Purpose: Implement locked fingerprint and restart decisions (D-05..D-08) plus restart trigger policy for RELO-01/RELO-02.
Output: Fingerprint package, supervisor restart gate wiring, and tests proving changed vs unchanged restart behavior.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-pid-1-supervision-reload-control/03-CONTEXT.md
@.planning/phases/03-pid-1-supervision-reload-control/03-RESEARCH.md
@.planning/phases/03-pid-1-supervision-reload-control/03-PATTERNS.md
@.planning/phases/03-pid-1-supervision-reload-control/03-01-PLAN.md

<interfaces>
From internal/state/store.go:
```go
func (s *Store) LastAppliedEnv() map[string]string
func (s *Store) LastRenderedFiles() map[string][]byte
```

From internal/sync/coordinator.go:
```go
type CycleResult struct {
    Status        CycleStatus
    AppliedEnv    []string
    RenderedFiles []string
}
```

From internal/supervisor/model.go (from Plan 03-01):
```go
type Reason string
const (
    ReasonConfigChanged Reason = "config_changed"
    ReasonChildExited   Reason = "child_exited"
    ReasonStartupFailed Reason = "startup_failed"
    ReasonForcedKill    Reason = "forced_kill"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for deterministic fingerprint and restart gating (RED)</name>
  <read_first>
    - tests/integration/pid1_supervision_reload_test.go
    - internal/supervisor/supervisor_test.go
    - internal/state/store.go
  </read_first>
  <files>
    - internal/fingerprint/fingerprint_test.go
    - tests/integration/pid1_supervision_reload_test.go
  </files>
  <behavior>
    - Test 1: identical logical env/files with different map insertion orders produce identical fingerprint.
    - Test 2: successful changed candidate state triggers exactly one restart.
    - Test 3: successful unchanged state does not restart child.
  </behavior>
  <action>
    Add unit tests for canonical deterministic hash behavior and integration tests for restart gating semantics. Encode D-05 (env + rendered bytes), D-06 (pre-commit candidate), D-07 (sorted canonicalization), and D-08 (metadata excluded) directly in assertions. Tests should fail before fingerprint package and gating logic are implemented. Analog anchors from `03-PATTERNS.md`: `internal/fingerprint/fingerprint_test.go` follows deterministic assertion style from `internal/exec/launcher_test.go` and state-handoff style from launcher/store-related tests (Pattern section `internal/fingerprint/fingerprint_test.go`), while restart-gate integration assertions in `tests/integration/pid1_supervision_reload_test.go` follow CLI harness and output assertions from `tests/integration/dual_vector_delivery_test.go` (Pattern section integration role-match).
  </action>
  <verify>
    <automated>go test ./internal/fingerprint ./tests/integration -run 'TestFingerprint_|TestPID1_Restart(OnChange|NoRestartOnSameFingerprint)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Fingerprint unit tests exist and assert deterministic equality/inequality cases.
    - Integration tests explicitly assert restart count/occurrence based on changed vs unchanged effective state.
    - At least one test fails pre-implementation.
  </acceptance_criteria>
  <done>RED tests define RELO-01/RELO-02 contract unambiguously.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical fingerprinting from pre-commit candidate state</name>
  <read_first>
    - internal/state/store.go
    - internal/sync/coordinator.go
    - internal/exec/launcher.go
    - .planning/phases/03-pid-1-supervision-reload-control/03-CONTEXT.md
  </read_first>
  <files>
    - internal/fingerprint/fingerprint.go
    - internal/sync/model.go
    - internal/sync/coordinator.go
  </files>
  <behavior>
    - Test 1: hash input includes only effective env payload + rendered file bytes.
    - Test 2: canonical ordering of env keys and file paths yields stable digest.
    - Test 3: runtime metadata (timestamps, latency, fetch order) do not affect digest.
  </behavior>
  <action>
    Create `internal/fingerprint` with canonical serialization and SHA-256 digest. Implement sorting by env key and output path before hashing (D-07). Hash must include effective env payload plus rendered file bytes (D-05), generated from candidate state before commit boundary (D-06). Do not include timestamps/latency/fetch order (D-08). Extend cycle/supervisor handoff structures so candidate fingerprint can be evaluated in restart gate logic. Analog anchors from `03-PATTERNS.md`: deterministic `sort.Strings` key/path ordering copied from `internal/exec/launcher.go` and shared deterministic-ordering pattern; immutable/stable snapshot mindset copied from `internal/state/store.go`; sorted surfaces and success-gated transition alignment copied from `internal/sync/coordinator.go` (`StageCandidate` -> `CommitCandidate` shared gate).
  </action>
  <verify>
    <automated>go test ./internal/fingerprint ./internal/sync -run 'Fingerprint|Cycle' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/fingerprint/fingerprint.go` exists and uses canonical sorting before SHA-256.
    - Fingerprint function accepts candidate env/files inputs and returns stable digest output.
    - No runtime metadata fields are included in fingerprint input.
  </acceptance_criteria>
  <done>Deterministic fingerprint engine exists and is wired for restart gating inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire restart-on-change gate in supervisor for successful cycles only</name>
  <read_first>
    - internal/supervisor/supervisor.go
    - internal/supervisor/model.go
    - internal/fingerprint/fingerprint.go
  </read_first>
  <files>
    - internal/supervisor/model.go
    - internal/supervisor/supervisor.go
    - tests/integration/pid1_supervision_reload_test.go
  </files>
  <action>
    Add supervisor gate logic: restart child only when cycle status is success AND candidate fingerprint differs from last applied fingerprint (RELO-02). Set restart reason `config_changed` when restart occurs (D-16). Keep unchanged-success cycles as no-op with child continuity. If child start fails during controlled restart, exit non-zero per D-14. Analog anchors from `03-PATTERNS.md`: restart gate must honor shared `Success-only commit gate` from `internal/sync/coordinator.go` (no restart on failed/non-committed cycle), use explicit reason taxonomy style from `internal/sync/model.go`/`internal/supervisor/model.go`, and preserve deterministic decision inputs from shared `Deterministic ordering before externally visible decisions` pattern.
  </action>
  <verify>
    <automated>go test ./internal/supervisor ./tests/integration -run 'RestartOnChange|NoRestartOnSameFingerprint|StartupFailed' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Restart occurs only on successful changed-state cycles.
    - No restart occurs on successful unchanged cycles.
    - Restart reason includes `config_changed` when triggered.
    - Controlled restart start failure exits non-zero.
  </acceptance_criteria>
  <done>RELO-01/RELO-02 behavior is runtime-enforced and test-backed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate state -> fingerprint digest | Secret-derived runtime data crosses into control decision logic |
| fingerprint gate -> restart action | Digest comparison drives process restarts impacting availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | `internal/fingerprint/fingerprint.go` input canonicalization | mitigate | Canonically sort env keys and output paths (D-07) and unit-test deterministic equivalence. |
| T-03-06 | Denial of Service | restart gate | mitigate | Restart only on success+change; unchanged cycles become no-op to prevent churn. |
| T-03-07 | Repudiation | restart reason semantics | mitigate | Record `config_changed` reason on change-triggered restart and assert in tests. |
| T-03-08 | Information Disclosure | fingerprint input material | mitigate | Hash in-memory payload only; never log raw env/file secret bytes. |
</threat_model>

<verification>
- `go test ./internal/fingerprint ./internal/supervisor ./tests/integration -run 'Fingerprint|Restart' -count=1`
- `go test ./...`
</verification>

<success_criteria>
- RELO-01: deterministic fingerprint implemented from effective candidate env+files.
- RELO-02: restart-on-change gate enforced for successful cycles only.
- Locked decisions D-05, D-06, D-07, D-08 are explicitly implemented and validated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pid-1-supervision-reload-control/03-02-SUMMARY.md`
</output>

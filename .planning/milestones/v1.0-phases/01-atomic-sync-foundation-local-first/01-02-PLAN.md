---
phase: 01-atomic-sync-foundation-local-first
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - internal/state/store.go
  - internal/clock/clock.go
  - internal/sync/model.go
  - internal/sync/coordinator.go
  - internal/sync/coordinator_test.go
  - tests/integration/atomic_sync_test.go
autonomous: true
requirements: [SYNC-03, SYNC-04]
must_haves:
  truths:
    - "If any path fetch fails, the cycle fails as one unit."
    - "If any path fetch exceeds timeout, the cycle aborts and reports deterministic failure/abort status."
    - "After failed cycle, previously applied state remains last-known-good and unchanged."
  artifacts:
    - path: "internal/state/store.go"
      provides: "Last-known-good snapshot store with candidate commit gate"
      contains: "CommitCandidate"
    - path: "internal/clock/clock.go"
      provides: "Testable timeout control for cycle workers"
      contains: "Now()"
    - path: "internal/sync/coordinator.go"
      provides: "Fail-fast abort + no-partial-commit behavior"
      contains: "context.WithTimeout"
    - path: "tests/integration/atomic_sync_test.go"
      provides: "Failure-path end-to-end proofs"
      contains: "TestAtomicSync_FailedCyclePreservesLastKnownGood"
  key_links:
    - from: "internal/sync/coordinator.go"
      to: "internal/state/store.go"
      via: "Only commit candidate on full-batch success"
      pattern: "CommitCandidate"
    - from: "internal/sync/coordinator.go"
      to: "context.WithTimeout"
      via: "Per-path timeout derived from cycle context"
      pattern: "WithTimeout"
---

## Phase Goal

**As a** developer, **I want to** run envfuse local cycles that abort on any fetch failure/timeout while preserving last-known-good state, **so that** failed sync attempts never produce partial state.

<objective>
Complete the Walking Skeleton failure-path slice for atomic guarantees.

Purpose: Deliver abort semantics and state-preservation invariants required by SYNC-03 and SYNC-04.
Output: Timeout/failure-aware coordinator + state store + failing/green tests proving no partial state mutation on failed cycle.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-RESEARCH.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-PATTERNS.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration tests for failure/timeout invariants (RED)</name>
  <read_first>
    - tests/integration/atomic_sync_test.go
    - internal/sync/model.go
    - internal/sync/coordinator.go
    - .planning/ROADMAP.md
  </read_first>
  <files>tests/integration/atomic_sync_test.go</files>
  <behavior>
    - Test 1: One failing path causes whole cycle failure; no new state commit occurs.
    - Test 2: One timed-out path aborts/failed cycle; no partial state commit occurs.
  </behavior>
  <action>Add two failing tests with concrete names: `TestAtomicSync_FailedCyclePreservesLastKnownGood` and `TestAtomicSync_TimeoutCyclePreservesLastKnownGood`. Use scenario: cycle A succeeds with value `v1`; cycle B has one failing/timed-out path and one valid path carrying `v2`; assert applied state remains `v1` after cycle B. Encode deterministic expected status (`failed` for fetch error and `aborted` for timeout cancellation) and reject any partial update assertions.</action>
  <acceptance_criteria>
    - `rg -n "TestAtomicSync_FailedCyclePreservesLastKnownGood|TestAtomicSync_TimeoutCyclePreservesLastKnownGood" tests/integration/atomic_sync_test.go` returns two test definitions.
    - `go test ./tests/integration -run "TestAtomicSync_(Failed|Timeout)CyclePreservesLastKnownGood" -count=1` fails before implementation.
  </acceptance_criteria>
  <verify>
    <automated>go test ./tests/integration -run "TestAtomicSync_(Failed|Timeout)CyclePreservesLastKnownGood" -count=1</automated>
  </verify>
  <done>Failure/timeout behavior is locked in as failing end-to-end tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fail-fast abort and last-known-good state store (GREEN)</name>
  <read_first>
    - internal/state/store.go
    - internal/sync/coordinator.go
    - internal/sync/model.go
    - tests/integration/atomic_sync_test.go
  </read_first>
  <files>internal/state/store.go, internal/sync/model.go, internal/sync/coordinator.go</files>
  <action>Implement `internal/state/store.go` with explicit candidate/apply boundary: keep `lastKnownGood` immutable during in-flight cycle, expose `StageCandidate` and `CommitCandidate` semantics, and never mutate applied state on failed/aborted cycles. Update coordinator to: (a) derive per-path timeout via `context.WithTimeout` from group context; (b) cancel siblings on first error; (c) classify timeout/cancel as `aborted` and direct fetch errors as `failed`; (d) call store commit only when all fetches succeed. Reference requirement behavior in comments with `per SYNC-03` and `per SYNC-04` for traceability.</action>
  <acceptance_criteria>
    - `rg -n "per SYNC-03|per SYNC-04" internal/sync/coordinator.go internal/state/store.go` returns traceability comments.
    - `rg -n "context.WithTimeout" internal/sync/coordinator.go` returns timeout control usage.
    - `go test ./tests/integration -run "TestAtomicSync_(Failed|Timeout)CyclePreservesLastKnownGood" -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./tests/integration -run "TestAtomicSync_(Failed|Timeout)CyclePreservesLastKnownGood" -count=1</automated>
  </verify>
  <done>Failed/aborted cycles are prevented from mutating applied state and integration tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic unit tests for cycle status + timeout edge behavior</name>
  <read_first>
    - internal/clock/clock.go
    - internal/sync/coordinator_test.go
    - internal/sync/coordinator.go
    - internal/sync/model.go
  </read_first>
  <files>internal/clock/clock.go, internal/sync/coordinator_test.go, internal/sync/coordinator.go</files>
  <behavior>
    - Test 1: First fetch error yields final cycle status `failed` and stops commit path.
    - Test 2: Timeout-derived cancellation yields final cycle status `aborted`.
  </behavior>
  <action>Create `internal/clock/clock.go` minimal abstraction (`Now`, optional `After`) to make timeout tests deterministic. Add unit tests `TestRunCycle_FirstErrorFailsCycle` and `TestRunCycle_TimeoutAbortsCycle` with stub providers controlling latency/errors. Assert exact status enums and assert commit callback/store is not invoked on failed/aborted cycles.</action>
  <acceptance_criteria>
    - `rg -n "TestRunCycle_FirstErrorFailsCycle|TestRunCycle_TimeoutAbortsCycle" internal/sync/coordinator_test.go` returns two test definitions.
    - `go test ./internal/sync -run "TestRunCycle_(FirstErrorFailsCycle|TimeoutAbortsCycle)" -count=1` passes.
    - `go test ./...` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/sync -run "TestRunCycle_(FirstErrorFailsCycle|TimeoutAbortsCycle)" -count=1 && go test ./...</automated>
  </verify>
  <done>Coordinator status transitions and no-partial-commit behavior are deterministic and fully test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider worker → cycle aggregator | Untrusted fetch outcomes (error/timeout/data) cross into commit decision |
| candidate state → applied state | Transition can corrupt runtime state if commit guard is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T (Tampering) | `internal/state/store.go` | mitigate | Enforce explicit stage/commit separation so failed cycle cannot alter applied state. |
| T-01-05 | D (Denial of Service) | `internal/sync/coordinator.go` | mitigate | Use per-path timeout and context cancellation to prevent stuck worker from blocking cycle completion. |
| T-01-06 | R (Repudiation) | `internal/sync/model.go` | mitigate | Record deterministic cycle statuses (`success`, `failed`, `aborted`) for auditable cycle outcomes. |
</threat_model>

<verification>
- `go test ./internal/sync -run "TestRunCycle_(FirstErrorFailsCycle|TimeoutAbortsCycle)" -count=1`
- `go test ./tests/integration -run "TestAtomicSync_(Failed|Timeout)CyclePreservesLastKnownGood" -count=1`
- `go test ./...`
</verification>

<success_criteria>
- Any fetch failure/timeout aborts the cycle as one unit.
- Last-known-good state remains unchanged after failed/aborted cycle.
- Requirements SYNC-03 and SYNC-04 are proven by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-sync-foundation-local-first/01-02-SUMMARY.md`
</output>

---
phase: 01-atomic-sync-foundation-local-first
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - cmd/envfuse/main.go
  - internal/config/config.go
  - internal/provider/provider.go
  - internal/provider/local/local.go
  - internal/sync/model.go
  - internal/sync/coordinator.go
  - internal/sync/coordinator_test.go
  - tests/integration/atomic_sync_test.go
autonomous: true
requirements: [PROV-03, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "**As a** developer, **I want to** run envfuse against local JSON secrets, **so that** I can validate sync behavior offline."
    - "A full configured path set is processed as one cycle result."
    - "Distinct secret paths are fetched concurrently in a successful cycle."
  artifacts:
    - path: "internal/config/config.go"
      provides: "Strict seon.json decode for local provider runtime"
      contains: "DisallowUnknownFields"
    - path: "internal/provider/local/local.go"
      provides: "Local JSON backend implementing provider contract"
      contains: "Fetch(ctx, resourceURI)"
    - path: "internal/sync/coordinator.go"
      provides: "Batch cycle orchestration with concurrent fetch fan-out"
      contains: "errgroup.WithContext"
    - path: "tests/integration/atomic_sync_test.go"
      provides: "Happy-path end-to-end proof for Phase 1 skeleton"
      contains: "TestAtomicSync_HappyPathBatchSuccess"
  key_links:
    - from: "cmd/envfuse/main.go"
      to: "internal/config/config.go"
      via: "Load config before cycle start"
      pattern: "LoadConfig"
    - from: "internal/sync/coordinator.go"
      to: "internal/provider/local/local.go"
      via: "Provider.Fetch for each distinct path"
      pattern: "Fetch\("
---

## Phase Goal

**As a** developer, **I want to** run envfuse from `seon.json` with a local backend and observe one deterministic successful batch cycle, **so that** I can trust the foundation before env/file materialization phases.

<objective>
Deliver the Walking Skeleton happy path: config load → local provider fetch → concurrent batch cycle success.

Purpose: Establish the first end-to-end vertical slice for Phase 1 and satisfy PROV-03, SYNC-01, SYNC-02 without partial feature placeholders.
Output: Runnable `cmd/envfuse` entrypoint with deterministic happy-path cycle plus integration + unit tests.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-RESEARCH.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-PATTERNS.md

<interfaces>
From `AGENTS.md` stack constraints:

```go
type Provider interface {
    Fetch(ctx context.Context, resourceURI string) (map[string]any, error)
}
```

Phase-1 cycle contract to implement in this plan:

```go
type CycleStatus string

const (
    CycleStatusSuccess CycleStatus = "success"
    CycleStatusFailed  CycleStatus = "failed"
    CycleStatusAborted CycleStatus = "aborted"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing happy-path integration test (RED)</name>
  <read_first>
    - tests/integration/atomic_sync_test.go
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>tests/integration/atomic_sync_test.go</files>
  <behavior>
    - Test 1: Given `seon.json` with two distinct local secret paths, one cycle returns `status=success`.
    - Test 2: Cycle reports both configured paths in a single batch result (not two independent cycle outcomes).
  </behavior>
  <action>Create `TestAtomicSync_HappyPathBatchSuccess` using temporary fixture files: `seon.json` + `secrets.json`. Use concrete config keys: `provider_type=local`, `local_file_path`, `secret_paths=["app/base","db/primary"]`. Assert expected cycle status and expected count of fetched paths. Keep the test failing first by calling not-yet-implemented runtime wiring (`RunSingleCycle` or equivalent) per TDD RED gate.</action>
  <acceptance_criteria>
    - `rg -n "TestAtomicSync_HappyPathBatchSuccess" tests/integration/atomic_sync_test.go` returns exactly one test definition.
    - `go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1` fails before implementation with unresolved/failed runtime behavior.
  </acceptance_criteria>
  <verify>
    <automated>go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1</automated>
  </verify>
  <done>Failing integration test exists and encodes the full happy-path batch behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement thinnest real happy-path vertical slice (GREEN)</name>
  <read_first>
    - internal/config/config.go
    - internal/provider/provider.go
    - internal/provider/local/local.go
    - internal/sync/model.go
    - internal/sync/coordinator.go
    - tests/integration/atomic_sync_test.go
  </read_first>
  <files>internal/config/config.go, internal/provider/provider.go, internal/provider/local/local.go, internal/sync/model.go, internal/sync/coordinator.go</files>
  <action>Implement concrete runtime contracts: (1) strict config parsing with `json.Decoder.DisallowUnknownFields`; (2) provider interface with `Fetch(ctx, resourceURI) (map[string]any, error)`; (3) local provider reading `secrets.json` and returning map values by path; (4) coordinator that deduplicates `secret_paths`, fans out concurrent fetch workers with `errgroup.WithContext`, aggregates results into one cycle result object. Do not add env/file apply logic in this plan. Keep logs/status secret-safe (path names + status only, no secret values) per project security constraint.</action>
  <acceptance_criteria>
    - `rg -n "DisallowUnknownFields" internal/config/config.go` returns a match.
    - `rg -n "errgroup.WithContext" internal/sync/coordinator.go` returns a match.
    - `rg -n "type Provider interface" internal/provider/provider.go` returns a match.
    - `go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1</automated>
  </verify>
  <done>Happy-path integration test is GREEN with real config→provider→coordinator runtime path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI wiring and concurrency proof test</name>
  <read_first>
    - cmd/envfuse/main.go
    - internal/sync/coordinator.go
    - internal/sync/coordinator_test.go
    - tests/integration/atomic_sync_test.go
  </read_first>
  <files>cmd/envfuse/main.go, internal/sync/coordinator_test.go, tests/integration/atomic_sync_test.go</files>
  <behavior>
    - Test 1: Distinct paths are fetched concurrently (proof via blocking stub provider + elapsed time/asserted ordering).
    - Test 2: CLI single-cycle run exits non-error on successful cycle.
  </behavior>
  <action>Create `cmd/envfuse/main.go` with concrete command shape: `envfuse -config ./seon.json -once`. Wire config load + local provider + coordinator single cycle execution. Add coordinator unit test `TestRunCycle_FetchesDistinctPathsConcurrently` using a stub provider that blocks each fetch until both start; verify two paths do not execute serially. Keep cycle output deterministic (`success|failed|aborted`).</action>
  <acceptance_criteria>
    - `rg -n "-once" cmd/envfuse/main.go` returns one flag wiring match.
    - `rg -n "TestRunCycle_FetchesDistinctPathsConcurrently" internal/sync/coordinator_test.go` returns one match.
    - `go test ./internal/sync -run TestRunCycle_FetchesDistinctPathsConcurrently -count=1` passes.
    - `go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/sync -run TestRunCycle_FetchesDistinctPathsConcurrently -count=1 && go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1</automated>
  </verify>
  <done>Walking skeleton happy-path is runnable from CLI and concurrency behavior is proven by automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → runtime | Untrusted local JSON input crosses into parser/runtime state |
| provider output → cycle model | Secret payload enters runtime decision path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | `internal/config/config.go` | mitigate | Enforce strict decode with `DisallowUnknownFields` and reject invalid schema before cycle start. |
| T-01-02 | I (Information Disclosure) | `cmd/envfuse/main.go`, `internal/sync/coordinator.go` | mitigate | Emit only path/status/error class logs; never include fetched secret values in output. |
| T-01-03 | D (Denial of Service) | `internal/sync/coordinator.go` | mitigate | Bound cycle execution via context-driven worker cancellation using `errgroup.WithContext`. |
</threat_model>

<verification>
- `go test ./internal/sync -run TestRunCycle_FetchesDistinctPathsConcurrently -count=1`
- `go test ./tests/integration -run TestAtomicSync_HappyPathBatchSuccess -count=1`
- `go test ./...`
</verification>

<success_criteria>
- Developer can run one local cycle from `seon.json` and receive deterministic `success` result.
- Runtime fetches configured distinct paths concurrently in one batch cycle.
- Requirements PROV-03, SYNC-01, SYNC-02 are verifiably covered by passing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-sync-foundation-local-first/01-01-SUMMARY.md`
</output>

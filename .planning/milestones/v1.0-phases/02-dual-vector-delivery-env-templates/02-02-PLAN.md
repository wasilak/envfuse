---
phase: 02-dual-vector-delivery-env-templates
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/integration/dual_vector_delivery_test.go
  - internal/config/config.go
  - internal/render/template.go
  - internal/fileio/path_guard.go
  - internal/fileio/atomic_writer.go
  - internal/state/store.go
  - internal/sync/model.go
  - internal/sync/coordinator.go
  - internal/sync/coordinator_test.go
autonomous: true
requirements: [INJT-02, INJT-03, INJT-04, SECU-03]
must_haves:
  truths:
    - "Operator can declare template outputs and get rendered files before child launch logic consumes them."
    - "Env payload and rendered files are derived from one successful cycle and committed together (never partially)."
    - "Missing required template keys fail startup/apply immediately."
    - "Runtime writes secrets only into declared target files and child env payload."
  artifacts:
    - path: internal/render/template.go
      provides: "strict template parse/execute with missingkey=error"
    - path: internal/fileio/path_guard.go
      provides: "declared-target allowlist and lexical path safety checks"
    - path: internal/fileio/atomic_writer.go
      provides: "temp-write, fsync, rename atomic file commit helper"
    - path: internal/sync/coordinator.go
      provides: "dual-vector plan build and single transaction commit gate"
    - path: tests/integration/dual_vector_delivery_test.go
      provides: "end-to-end dual-vector render/write success/failure invariants"
  key_links:
    - from: internal/sync/coordinator.go
      to: internal/render/template.go
      via: "single-snapshot template render planning"
      pattern: "Render.*missingkey=error"
    - from: internal/sync/coordinator.go
      to: internal/fileio/atomic_writer.go
      via: "commit rendered files only after complete validation"
      pattern: "WriteAtomic|Commit"
    - from: internal/fileio/path_guard.go
      to: internal/config/config.go
      via: "declared output targets allowlist"
      pattern: "template targets|allowlist"
---

<objective>
Complete the Phase 2 core dual-vector slice by adding strict template rendering and file materialization, then bind env + file outputs into one atomic commit path.

Purpose: satisfy INJT-02/03/04 and SECU-03 file boundary guarantees while preserving Phase 1 no-partial-commit semantics.
Output: template config schema, renderer, safe file writer, coordinator dual-vector transaction, and end-to-end dual-vector tests.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-RESEARCH.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-PATTERNS.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-01-PLAN.md

<interfaces>
From internal/sync/coordinator.go:
```go
func (c *Coordinator) RunCycle(ctx context.Context, secretPaths []string) CycleResult
```

From cmd/envfuse/main.go:
```go
func main()
```

From internal/sync/model.go:
```go
type CycleResult struct {
    Status       CycleStatus
    FetchedPaths []string
    ErrorClass   string
}
```

From internal/config/config.go (to extend, not replace):
```go
type Config struct {
    ProviderType  string   `json:"provider_type"`
    LocalFilePath string   `json:"local_file_path"`
    SecretPaths   []string `json:"secret_paths"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing dual-vector end-to-end tests for templates + atomic commit (RED)</name>
  <read_first>
    - tests/integration/dual_vector_delivery_test.go
    - tests/integration/atomic_sync_test.go
    - internal/sync/coordinator.go
  </read_first>
  <files>
    - tests/integration/dual_vector_delivery_test.go
  </files>
  <behavior>
    - Test 1: success cycle materializes both env payload and rendered file outputs from same snapshot.
    - Test 2: missing required template key returns non-success and commits neither env nor files.
    - Test 3: undeclared/unsafe output path is rejected before file write.
  </behavior>
  <action>
    Extend the integration suite with explicit dual-vector cases:
    happy path where env and files are both produced together,
    missing-key failure path validating INJT-04,
    path-boundary failure validating SECU-03.
    Tests must assert atomic invariants: previous last-known-good outputs remain unchanged after any failure.
    Pattern analog (02-PATTERNS.md): `tests/integration/dual_vector_delivery_test.go` role-match analog to `tests/integration/atomic_sync_test.go` (fixture setup + success/failure invariant structure).
  </action>
  <verify>
    <automated>go test ./tests/integration -run 'TestDualVector_(Templates|Atomic|MissingKey|PathGuard)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - New integration cases compile and fail against pre-implementation behavior.
    - Each failure test asserts both status/error class and no-partial-state mutation.
    - Happy-path test asserts both channels (env + file) are materialized together.
  </acceptance_criteria>
  <done>Executable failing tests define the complete Phase 2 dual-vector contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement template engine, safe file output, and unified dual-vector commit gate (GREEN)</name>
  <read_first>
    - internal/config/config.go
    - internal/state/store.go
    - internal/sync/coordinator.go
    - .planning/phases/02-dual-vector-delivery-env-templates/02-RESEARCH.md
  </read_first>
  <files>
    - internal/config/config.go
    - internal/render/template.go
    - internal/fileio/path_guard.go
    - internal/fileio/atomic_writer.go
    - internal/state/store.go
    - internal/sync/model.go
    - internal/sync/coordinator.go
  </files>
  <behavior>
    - Test 1: templates execute with `missingkey=error`; unresolved key fails cycle.
    - Test 2: rendered output targets pass declared allowlist + lexical path checks before writes.
    - Test 3: env and file plans commit as one transaction after full validation only.
  </behavior>
  <action>
    Extend config with template declarations (inline/template source, output target, required paths).
    Implement renderer using `text/template` with `Option("missingkey=error")` and deterministic error wrapping.
    Implement file path guard enforcing declared-target allowlist and lexical safety (`filepath.Clean`/`IsLocal` pattern per research).
    Implement atomic writer helper (`tmp write -> fsync -> rename`) for each declared output file.
    Update state model and coordinator to stage both env payload and file outputs from one fetched snapshot and commit them together in a single success gate.
    On any render/path/write/validation failure, return deterministic failure class and keep last-known-good unchanged.
    Pattern analogs (02-PATTERNS.md):
    - `internal/config/config.go` exact analog for strict decode + explicit validation errors.
    - `internal/render/template.go` dataflow-match analog to `internal/provider/local/local.go` (pure transform + defensive copy/error wrapping).
    - `internal/fileio/path_guard.go` dataflow-match analog to config guard-clause style (sequential explicit rejects).
    - `internal/fileio/atomic_writer.go` partial analog to `internal/state/store.go` two-phase stage/commit contract semantics.
    - `internal/state/store.go` exact analog for candidate vs last-known-good clone/commit behavior.
    - `internal/sync/model.go` exact analog for status/error-class preservation while adding dual-vector metadata.
    - `internal/sync/coordinator.go` exact analog for errgroup fetch orchestration and single commit gate, now covering env+files together.
  </action>
  <verify>
    <automated>go test ./internal/render ./internal/fileio ./internal/sync ./tests/integration -run 'DualVector|Template|MissingKey|PathGuard|Atomic' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `missingkey=error` is active for all runtime template executions (INJT-04).
    - Successful cycle produces both applied env payload and rendered files in one commit (INJT-03).
    - Runtime writes only to declared target files and never outside allowlist (SECU-03).
    - Failed cycle leaves both previously applied env and files unchanged.
  </acceptance_criteria>
  <done>Dual-vector delivery is fully implemented with strict validation and atomic semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic unit tests for render/path-guard edge cases and coordinator error classes</name>
  <read_first>
    - internal/sync/coordinator_test.go
    - internal/render/template.go
    - internal/fileio/path_guard.go
  </read_first>
  <files>
    - internal/sync/coordinator_test.go
  </files>
  <action>
    Add unit tests pinning edge behavior:
    template missing-key classification,
    unsafe target path rejection,
    commit suppression on any dual-vector validation failure,
    deterministic status/error_class mapping for troubleshooting.
    Pattern analog (02-PATTERNS.md): `internal/sync/coordinator_test.go` exact analog for concurrent cycle assertions and no-commit-on-failure invariants.
  </action>
  <verify>
    <automated>go test ./internal/sync -run 'TestRunCycle_(TemplateMissingKey|DisallowedTarget|DualVectorCommitGate)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests assert error classes for template and path validation failures.
    - Unit tests prove no commit occurs when either env or file branch fails.
    - Test names and assertions describe dual-vector invariants clearly.
  </acceptance_criteria>
  <done>Regression guardrails exist for all critical dual-vector failure modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config template targets -> filesystem writer | Untrusted config paths cross into privileged file write operations |
| provider snapshot -> template renderer | Secret payload enters templating and may fail/omit required keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | `internal/fileio/path_guard.go` | mitigate | Enforce declared-target allowlist + lexical path normalization checks before any write. |
| T-02-05 | Information Disclosure | file materialization path | mitigate | Restrict writes to declared output files only; never log rendered secret values. |
| T-02-06 | Denial of Service | `internal/render/template.go` missing-key execution | mitigate | Fail fast with deterministic error class and preserve last-known-good outputs. |
| T-02-07 | Elevation of Privilege | coordinator dual-branch apply ordering | mitigate | Single transaction gate commits env+files together; reject any partial branch success. |
</threat_model>

<verification>
Run complete phase-targeted verification:
- `go test ./internal/... ./tests/integration -run 'DualVector|Template|Atomic|MissingKey|PathGuard' -count=1`
</verification>

<success_criteria>
- INJT-02 complete: templates render to declared files before supervisor consumption.
- INJT-03 complete: env+files committed in one successful cycle only.
- INJT-04 complete: missing template keys fail startup/apply deterministically.
- SECU-03 complete for file channel: writes constrained to declared output targets.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-vector-delivery-env-templates/02-02-SUMMARY.md`
</output>

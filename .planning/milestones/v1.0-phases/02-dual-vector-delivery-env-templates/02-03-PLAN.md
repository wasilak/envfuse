---
phase: 02-dual-vector-delivery-env-templates
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [INJT-01, INJT-02, INJT-03, INJT-04, SECU-03]
must_haves:
  truths:
    - "Phase 2 MVP goal is written in valid User Story format (`As a ..., I want to ..., so that ...`)."
    - "Phase 2 verification is re-run after goal correction and no longer reports the MVP goal-format blocker."
    - "Phase 2 requirement coverage remains explicit for INJT-01, INJT-02, INJT-03, INJT-04, and SECU-03."
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Phase 2 goal corrected to valid MVP user-story syntax"
    - path: .planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
      provides: "Re-verified phase report reflecting corrected goal guard and updated status"
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
      via: "goal text validated with user-story guard and reflected in verification evidence"
      pattern: "gsd-sdk query user-story.validate"
---

<objective>
Close the only open Phase 2 verification gap by correcting the MVP goal format and immediately re-verifying the phase.

Purpose: remove the verification blocker without changing already-passing implementation behavior.
Output: corrected roadmap goal plus refreshed verification report with updated gap status.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-01-PLAN.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Phase 2 MVP goal to valid User Story syntax in ROADMAP</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
  </read_first>
  <files>
    - .planning/ROADMAP.md
  </files>
  <action>
    Replace the existing Phase 2 goal sentence with a valid user story in the required form:
    "As a platform operator, I want envfuse to materialize secrets into both child environment variables and rendered template files within one validated sync cycle, so that workloads start with complete and deterministic configuration without partial secret state."
    Keep `Mode: mvp` unchanged and keep existing requirement IDs unchanged.
    Do not modify implementation scope, phase ordering, or success criteria beyond wording needed to satisfy the goal-format guard.
  </action>
  <verify>
    <automated>gsd-sdk query user-story.validate --story "As a platform operator, I want envfuse to materialize secrets into both child environment variables and rendered template files within one validated sync cycle, so that workloads start with complete and deterministic configuration without partial secret state."</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 2 `Goal` line is in `As a..., I want to..., so that...` structure.
    - Validator output confirms `valid: true` for the exact goal text in ROADMAP.
    - Existing Phase 2 requirement IDs remain `INJT-01, INJT-02, INJT-03, INJT-04, SECU-03`.
  </acceptance_criteria>
  <done>Phase 2 MVP goal-format blocker is removed at source.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run verification and refresh 02-VERIFICATION.md to close the recorded gap</name>
  <read_first>
    - .planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
    - .planning/phases/02-dual-vector-delivery-env-templates/02-02-SUMMARY.md
    - .planning/ROADMAP.md
  </read_first>
  <files>
    - .planning/phases/02-dual-vector-delivery-env-templates/02-VERIFICATION.md
  </files>
  <action>
    Re-run Phase 2 verification with the corrected goal text and update `02-VERIFICATION.md` frontmatter/body accordingly:
    set status/score based on the new run, remove or mark resolved the MVP goal-format gap, and keep existing technical evidence for INJT-01/02/03/04 + SECU-03 intact unless fresh verification indicates regressions.
    Ensure the report clearly shows that verification was re-run after goal correction (per gap missing item "Re-run verification after goal format correction").
  </action>
  <verify>
    <automated>go test ./internal/... ./tests/integration -run 'DualVector|Template|Atomic|Child(Start|Restart)Env|MissingKey|PathGuard' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `02-VERIFICATION.md` reflects a post-fix verification run timestamp and outcome.
    - MVP goal-format guard is no longer listed as failed in `gaps`.
    - Requirement coverage table still explicitly maps INJT-01, INJT-02, INJT-03, INJT-04, and SECU-03.
  </acceptance_criteria>
  <done>Gap report is closed or updated with current truth after mandatory re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts -> verification status | Incorrect documentation edits can misrepresent real phase readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | `.planning/ROADMAP.md` Phase 2 goal | mitigate | Validate corrected goal with `gsd-sdk query user-story.validate` before saving verification outcome. |
| T-02-10 | Repudiation | `02-VERIFICATION.md` rerun evidence | mitigate | Include explicit rerun timestamp + commands and preserve requirement evidence table for auditability. |
</threat_model>

<verification>
- `gsd-sdk query user-story.validate --story "As a platform operator, I want envfuse to materialize secrets into both child environment variables and rendered template files within one validated sync cycle, so that workloads start with complete and deterministic configuration without partial secret state."`
- `go test ./internal/... ./tests/integration -run 'DualVector|Template|Atomic|Child(Start|Restart)Env|MissingKey|PathGuard' -count=1`
</verification>

<success_criteria>
- Phase 2 goal-format gap from `02-VERIFICATION.md` is resolved.
- Verification report is current and no longer blocked by MVP syntax guard.
- Requirement coverage for INJT-01, INJT-02, INJT-03, INJT-04, SECU-03 remains explicit and unchanged in scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-vector-delivery-env-templates/02-03-SUMMARY.md`
</output>

---
phase: 02-dual-vector-delivery-env-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/dual_vector_delivery_test.go
  - internal/config/config.go
  - internal/inject/mapping.go
  - internal/inject/validation.go
  - internal/state/store.go
  - internal/sync/model.go
  - internal/sync/coordinator.go
  - internal/sync/coordinator_test.go
autonomous: true
requirements: [INJT-01, SECU-03]
must_haves:
  truths:
    - "Operator can define path:key->env mappings in seon.json and see them materialized as child-env payload after a successful cycle."
    - "Failed env mapping resolution does not mutate last-known-good applied outputs."
    - "Only explicitly declared env targets are materialized (no implicit/global env mutation)."
  artifacts:
    - path: tests/integration/dual_vector_delivery_test.go
      provides: "failing-then-passing end-to-end proof for env mapping delivery"
    - path: internal/inject/mapping.go
      provides: "path:key mapping resolver from fetched snapshot to env payload"
    - path: internal/config/config.go
      provides: "strict schema for env mapping declarations"
    - path: internal/state/store.go
      provides: "stage/commit persistence for applied env payload"
  key_links:
    - from: internal/config/config.go
      to: internal/inject/mapping.go
      via: "validated mapping structs consumed by resolver"
      pattern: "EnvMappings|path:key|env_var"
    - from: internal/sync/coordinator.go
      to: internal/state/store.go
      via: "single commit gate after full env-plan validation"
      pattern: "StageCandidate|CommitCandidate"
    - from: internal/sync/coordinator.go
      to: internal/inject/mapping.go
      via: "build child env payload from same fetched snapshot"
      pattern: "Resolve.*Env|mapping"
---

<objective>
Deliver the first MVP vertical slice for Phase 2: env mapping declarations are validated and materialized into a child-environment payload inside the existing atomic cycle boundary.

Purpose: establish INJT-01 on top of Phase 1 all-or-nothing semantics, without introducing global environment leakage (SECU-03).
Output: strict env mapping schema, resolver/validator, and committed applied-env payload with end-to-end test proof.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-RESEARCH.md
@.planning/phases/02-dual-vector-delivery-env-templates/02-PATTERNS.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-01-SUMMARY.md
@.planning/phases/01-atomic-sync-foundation-local-first/01-02-SUMMARY.md

<interfaces>
From internal/config/config.go:
```go
type Config struct {
    ProviderType  string   `json:"provider_type"`
    LocalFilePath string   `json:"local_file_path"`
    SecretPaths   []string `json:"secret_paths"`
}
```

From internal/sync/model.go:
```go
type CycleStatus string

const (
    CycleStatusSuccess CycleStatus = "success"
    CycleStatusFailed  CycleStatus = "failed"
    CycleStatusAborted CycleStatus = "aborted"
)

type CycleResult struct {
    Status       CycleStatus
    FetchedPaths []string
    ErrorClass   string
}
```

From internal/state/store.go:
```go
func (s *Store) StageCandidate(candidate map[string]map[string]any)
func (s *Store) CommitCandidate()
func (s *Store) LastKnownGood() map[string]map[string]any
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing end-to-end env mapping slice test (RED)</name>
  <read_first>
    - tests/integration/atomic_sync_test.go
    - internal/sync/coordinator.go
    - internal/config/config.go
  </read_first>
  <files>
    - tests/integration/dual_vector_delivery_test.go
  </files>
  <action>
    Create integration tests that currently fail and encode the Phase 2 slice behavior:
    (1) successful cycle with `env_mappings` resolves declared `path:key` pairs into an applied child-env payload,
    (2) missing mapping key fails cycle with deterministic error class,
    (3) failed cycle preserves last-known-good env payload unchanged.
    Reuse the existing scripted-provider fixture style from `atomic_sync_test.go` instead of introducing a new test harness.
    Pattern analog (02-PATTERNS.md): `tests/integration/dual_vector_delivery_test.go` -> `tests/integration/atomic_sync_test.go` (fixture + cycle invariant assertions).
  </action>
  <verify>
    <automated>go test ./tests/integration -run 'TestDualVector_EnvMapping' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists with at least one happy-path and one failure-path env-mapping test.
    - Tests fail against current codebase before implementation changes.
    - Failure assertions check both status/error_class and no-mutation invariant.
  </acceptance_criteria>
  <done>Executable failing tests define MVP env-mapping behavior contract for INJT-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict env mapping schema + resolver in atomic cycle (GREEN)</name>
  <read_first>
    - internal/config/config.go
    - internal/sync/coordinator.go
    - internal/state/store.go
    - .planning/phases/02-dual-vector-delivery-env-templates/02-RESEARCH.md
  </read_first>
  <files>
    - internal/config/config.go
    - internal/inject/mapping.go
    - internal/inject/validation.go
    - internal/state/store.go
    - internal/sync/model.go
    - internal/sync/coordinator.go
  </files>
  <behavior>
    - Test 1: valid `env_mappings` produce deterministic child-env payload from the same fetched snapshot.
    - Test 2: unresolved `path:key` fails cycle before commit.
    - Test 3: failed mapping resolution leaves applied state at previous last-known-good values.
  </behavior>
  <action>
    Extend config schema with explicit env mapping declarations (`path`, `key`, `env_var`) and strict validation rules.
    Implement `internal/inject` resolver that consumes one immutable fetched snapshot and returns a deduplicated env payload keyed by declared `env_var` only.
    Update state and sync models so the coordinator stages/commits applied env payload in the same transaction gate used in Phase 1.
    Keep Phase 1 fetch behavior and status semantics intact; add new deterministic `ErrorClass` values for mapping validation failures.
    Do not call `os.Setenv` or mutate process-global environment (SECU-03); only store child-env payload for later supervisor use.
     Pattern analogs (02-PATTERNS.md):
     - `internal/config/config.go` exact analog for strict decode + guard-clause validation (`DisallowUnknownFields`, explicit required-field errors).
     - `internal/inject/mapping.go` role-match analog from config validation error style (deterministic wrapped errors).
     - `internal/inject/validation.go` dataflow-match analog to `internal/config/config.go` for sequential guard clauses + deterministic validation error strings.
     - `internal/state/store.go` exact analog for stage/commit transaction semantics and clone-snapshot safety.
     - `internal/sync/model.go` exact analog for status/error-class typing (extend metadata, preserve semantics).
     - `internal/sync/coordinator.go` exact analog for errgroup fetch + single commit gate (stage complete plan first, commit once).
   </action>
  <verify>
    <automated>go test ./internal/sync ./internal/inject ./tests/integration -run 'EnvMapping|DualVector' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `seon.json` accepts and validates env mappings with `DisallowUnknownFields` preserved.
    - Coordinator returns success only when env plan resolves completely.
    - Coordinator commit path updates applied env payload only after full-cycle success.
    - No code path writes mapped secrets to global process env.
  </acceptance_criteria>
  <done>Phase 2 env-mapping slice is implemented and passing previously failing integration tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic unit coverage for mapping validation and no-leak guarantees</name>
  <read_first>
    - internal/sync/coordinator_test.go
    - internal/state/store.go
    - internal/inject/mapping.go
  </read_first>
  <files>
    - internal/sync/coordinator_test.go
  </files>
  <action>
    Add focused unit tests that lock resolver and coordinator invariants:
    duplicate env_var handling, missing path/key classification, and no-commit-on-validation-failure.
    Assert that only declared env vars appear in committed payload and that unrelated host env keys are never injected implicitly.
    Pattern analog (02-PATTERNS.md): `internal/sync/coordinator_test.go` exact analog for failure invariant assertions (`Status/ErrorClass` + last-known-good unchanged on failed cycle).
  </action>
  <verify>
    <automated>go test ./internal/sync -run 'TestRunCycle_EnvMapping|TestRunCycle_NoCommitOnEnvValidationFailure' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests pin expected error_class values for env validation failures.
    - Unit tests verify committed payload contains only declared env_var keys.
    - Unit tests verify previous committed state remains unchanged after failed cycle.
  </acceptance_criteria>
  <done>Env mapping behavior is deterministic and regression-protected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file -> runtime validator | Untrusted operator input (`env_mappings`) enters trusted runtime state machine |
| provider payload -> env resolver | Secret data from provider is transformed into child-env payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `internal/config/config.go` env mapping decode | mitigate | Keep strict JSON decode with `DisallowUnknownFields`; reject malformed mapping schema at load time. |
| T-02-02 | Information Disclosure | env materialization path | mitigate | Never call `os.Setenv`; store only child-scoped env payload for supervised child launch path (SECU-03). |
| T-02-03 | Repudiation | cycle failure reason classification | mitigate | Emit deterministic `ErrorClass` values for env mapping failures and assert in tests. |
</threat_model>

<verification>
Run full targeted suite after task completion:
- `go test ./internal/inject ./internal/sync ./tests/integration -run 'DualVector|EnvMapping' -count=1`
</verification>

<success_criteria>
- INJT-01 satisfied with integration + unit proof of path:key-to-env mapping resolution.
- SECU-03 partially satisfied for env channel: no global env mutation, only declared env_var outputs.
- Last-known-good atomic semantics preserved for env mapping failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-vector-delivery-env-templates/02-01-SUMMARY.md`
</output>
